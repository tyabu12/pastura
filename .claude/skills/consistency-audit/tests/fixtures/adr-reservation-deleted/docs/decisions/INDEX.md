# Decision index

## ADR-001 — Architecture overview

Backed by a file on disk, so nothing fires for it.

## ADR-006 — Cloud API implementation details

Listed here with no file on disk and nothing in CLAUDE.md's table, so the
canary must fire. The heading itself is also a dangling reference — the flood
the canary exists to explain.
