# Decision index

## ADR-001 — Architecture overview

Agrees with the roster and has a file. Silent.

## ADR-002 — Multi-platform strategy

Has a file and a heading here, but the roster was never appended. This is the
case the two-way roster-vs-index comparison would have missed only if the
index had ALSO been skipped — covered by ADR-003 below.

## ADR-006 — Cloud API

Listed with no file on disk, and CLAUDE.md carries the reserved row, so both
the roster check and the reservation canary must stay silent.

## ADR-007 — Focus mode, revised

The roster still says "Focus mode". One side was edited without the other.

## ADR-016 — Home redesign — bottom-tab IA

The title carries its own em dash after the separator.
