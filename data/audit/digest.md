# Consistency audit — digest

Run summaries appended by `/consistency-audit`, newest first. Manual runs
(and, once scheduled, routine runs) leave this file modified in the main
checkout's working tree; committing it goes through the normal `/orchestrate`
flow (mirrors `data/queue/digest.md`).

<!-- audit-digest:sections -->
