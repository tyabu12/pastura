# Overnight Issue Queue — digest

Run summaries appended by `/queue-consumer`, newest first. Nightly runs
leave this file modified in the main checkout's working tree; committing
it goes through the normal `/orchestrate` flow (mirrors
`data/factory/digest.md`).

<!-- queue-digest:sections -->
