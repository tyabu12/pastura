# Both-listed fixture

ADR-099 is in the roster and in INDEX.md with no file and no reserved row —
the shape `adr-writing.md` §4 produces, since it requires both listings. That
is exactly what unparsed_adr_reservation reports, with a reservation-specific
action, so the roster check must defer rather than file a second issue for the
same omission on top of the dangling_adr the canary already explains.

### ADR roster

001 Architecture overview · 099 Withdrawn experiment

## Expected

The canary and the dangling reference it explains. No adr_roster_drift.
