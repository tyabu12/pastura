# ADR-021: Graceful Degradation of LLM Turn Failures

> **Status:** Accepted
> **Date:** 2026-07-08
> **Context:** A single LLM-call failure anywhere in a run aborts the whole simulation (#992); runs cost minutes to tens of minutes, and the app is pre-submission with zero install base, so a late-run total loss for a first real user (App Review included) is a launch-critical risk.

## Context

### The all-or-nothing failure path today

Every phase handler's throw is caught in exactly one place — the catch-all in
`SimulationRunner.executePhases` (`Pastura/Pastura/Engine/SimulationRunner.swift:357-375`) —
which emits `.error(...)` and returns `true`, terminating the round loop. `.simulationCompleted`
is never reached, and the App layer's terminal-status precedence
(`SimulationViewModel.terminalStatus`, `SimulationViewModel.swift:1279-1288`) maps any
non-cancelled `.error` to `SimulationStatus.failed`. There is no per-phase or per-turn
containment: one flaky parse in round 3 of 10 marks the entire run failed.

`Engine/LLMCaller.swift` retries each turn up to `maxRetries = 2` (3 attempts, shared budget
across four retry causes: `sampler_crash`, `parse_failed`, `empty_field`, `language_mismatch`).
Exhaustion outcomes are already asymmetric:

| Cause | On budget exhaustion | Run survives? |
|---|---|---|
| `parse_failed` | `throw SimulationError.retriesExhausted` (`LLMCaller.swift:114-121`) | **No** |
| stream/generation error (non-retryable) | `throw SimulationError.llmGenerationFailed` (`LLMCaller.swift:98`) | **No** |
| `empty_field` | falls through, returns the empty-field output (`LLMCaller.swift:127-131`) | Yes (silent) |
| `language_mismatch` | emits informational `.languageMismatch` event, returns output (`LLMCaller.swift:140-145`) | **Yes (explicit)** |

(**2026-08-06** — the `empty_field` row is the state this ADR was written against; its
"Yes (silent)" is now the *defect* rather than the design. Exhaustion throws when the declared
schema carries the canonical primary and its value is empty. The other three rows stand. See
§ "Amendment 2026-08-06".)

Two error classes already bypass the budget by design: `LLMError.suspended` is consumed
inside `consumeStreamWithSuspendRetry` (background/foreground cycles are free), and
`LLMError.invalidGrammar` is fail-fast in the budget sense — a deterministic engineering bug
must not burn 3 flaky-inference retries. Note, however, that today *every* non-retryable
stream error — `invalidGrammar` and `notLoaded` included — is still type-erased into
`SimulationError.llmGenerationFailed(description:)` at `LLMCaller.swift:98`; fail-fast means
"no retry", not "escapes typed". D3 below changes that wrap.

### The codebase is already half-defensive

The Engine's **code phases and downstream consumers already tolerate missing inputs**
(verified per consumer at draft time):

- `VoteHandler` drops out-of-candidate ballots from the tally while keeping the raw value
  for observability (`VoteHandler.swift:65-74`, #524).
- `ChooseHandler.validateAction` falls back to `options[0]` for off-menu actions
  (`ChooseHandler.swift:156-159`, #599).
- `EliminateHandler` no-ops on empty `voteResults`; `WordwolfJudgeLogic` emits a
  "No votes recorded" summary and skips scoring; `VoteTallyLogic` ignores unknown names;
  `EventReactivePayoffLogic` no-ops on a missing favored variable.
- `ConditionEvaluator` resolves absent variables (e.g. `vote_winner` with no votes) to a
  `false` comparison with a `.summary` warning — routing to the `else` branch, never throwing.
- `ReflectHandler` writes `notes_<name>` only when non-empty, so a failed/empty inference
  cannot erase a previous round's memo (`ReflectHandler.swift:65-72`).
  (**2026-08-06** — still true, but demoted from precedent to defense-in-depth: for a
  *declared* `note` the exhaustion path can no longer hand this guard an empty value. See
  § "Amendment 2026-08-06".)

Only the six LLM phases (`PhaseType.requiresLLM == true`: `speak_all`, `speak_each`, `vote`,
`choose`, `reflect`, `whisper` — `PhaseType.swift:51-59`) lack containment, and
`.languageMismatch` (`SimulationEvent.swift:163-183`) already models the target pattern:
an **informational event + continue**, explicitly documented as "unlike `.error`".

### Partial-progress durability already exists

`TurnRecord` rows persist per-output through a serial queue regardless of the run's eventual
status, and `.roundCheckpoint(state:)` persists a resumable snapshot at each round boundary.
When a run dies, everything up to the failure is already durably stored — the missing pieces
are (a) not throwing the rest of the run away, and (b) an honest status/display for the gap.

### Related

- #751 — recoverable parse flakiness (stray trailing `}`, empty output); the dominant
  *transient* failure class this ADR contains. Parser-side fixes remain tracked there.
- #680 — Metal reload hang (closed); an example of the *systemic* class this ADR must NOT
  paper over turn-by-turn.
- ADR-017 — `SuspendController`: the existing precedent for "interrupt without failing the run".

## Options

### Containment altitude

| Option | Pros | Cons |
|--------|------|------|
| **A. Runner-level phase skip** — catch in `executePhases`, consult a per-`PhaseType` policy table, skip the phase | Single choke point; no handler edits | Whole-phase granularity: one agent's flaky turn discards all other agents' work in that phase; cannot express per-pair (choose) or per-ballot (vote) semantics |
| **B. Handler-level per-turn containment** (chosen) | Matches the natural failure unit (one agent × one phase); aligns with existing per-ballot/per-action precedents; other agents' turns in the same phase survive | Touches all six LLM handlers (mitigated by a shared gate helper) |
| **C. `LLMCaller` returns a degraded marker instead of throwing** | No handler catch logic | Pushes phase semantics into a layer that has none; every call site must branch on the marker; blurs the throw contract with systemic errors |
| **D. Status quo + resume-proposal only** | Smallest diff | Does not help the common case (one transient flake mid-run still kills the run; resume costs a user round-trip and re-runs the whole interrupted round) |

### Degraded-run status representation

| Option | Pros | Cons |
|--------|------|------|
| **New `SimulationStatus.completedWithGaps`** | Queryable at the status level | Ripples every status switch, filter, and localization for what is a quality annotation, not a lifecycle state; blurs resume/cancel logic |
| **Keep 5-way status + degradation metadata on the record** (chosen) | Status stays a pure lifecycle taxonomy; additive column migration; UI badge drives honesty | "Has gaps" needs a column read, not a status compare |

## Decision

**D1 — Contain failures per turn, inside the LLM handlers.** A failed LLM call degrades to a
*skipped turn*: that agent's contribution to that phase is omitted and the phase continues
with the remaining agents/pairs. The phase still emits `.phaseCompleted`; the round and run
continue. Implemented via a single shared gate (working name `TurnFailureGate`, an
Engine-layer type carried on `PhaseContext`) so the catch/classify/count logic exists once,
not six times:

```swift
guard let output = try await context.turnGate.attempt(
  agent: persona.name, phaseType: context.phase.type, emitter: context.emitter,
  work: { try await llmCaller.call(...) }
) else { continue }  // turn skipped — gate already emitted .turnSkipped
```

**D2 — Degrade by omission, never fabrication.** A skipped turn writes nothing synthetic:

- `speak_all` / `speak_each`: no `conversationLog` entry, no `.agentOutput`.
- `vote`: abstention — no ballot recorded, no tally contribution (extends the #524 tally-drop
  precedent from "invalid ballot" to "no ballot").
- `choose` (round-robin): the **whole pairing is dropped** — a pairing with one real and one
  defaulted action would flow into `PrisonersDilemmaLogic`'s `?? "cooperate"` default, i.e.
  fabrication by another name. The partner's already-emitted utterance stays visible, and the
  partner's successful action remains in `state.lastOutputs` (legitimately consumable by
  `EventReactivePayoffLogic`); only the *pair-based* scoring interaction is omitted. The
  `lastOutputs` clear below applies to the **failed member only**. The #599 `options[0]`
  fallback is untouched — it handles a *delivered but off-menu* answer; a failed call
  delivers nothing to canonicalize.
  **Superseded 2026-07-17** — the boundary drawn here is real, but "untouched" was the wrong
  conclusion: `options[0]` fabricates too (an agent emitting `betray!` is scored as
  cooperating). `validateAction` now normalizes, returns the canonical option, and drops the
  pairing on a genuine miss. See § "Amendment 2026-07-17".
- `choose` (individual) / `reflect`: no write for that agent/turn.
- `whisper`: a skipped utterance **ends that pair's exchange early** — the remaining
  sub-round turns for that pair are not attempted (a partner "replying" to a missing
  utterance would desync the exchange). Turns already exchanged stand, and the
  `whispers_<name>` keys update under the existing overwrite semantics.

Additionally, a skipped turn in a `lastOutputs`-writing phase (speak/vote/choose) **clears**
`state.lastOutputs[agent]` rather than leaving the previous phase's entry readable: stale
decision data must not leak into consumers keyed on it (`EventReactivePayoffLogic` reads
`lastOutputs[name].action`; `RelationshipUpdateHandler` reads `.vote` — a stale prior-round
action would be silently scored as current). Private-memory keys (`notes_`, `whispers_`,
`relationships_`) retain their previous values, consistent with the existing `ReflectHandler`
non-empty guard: memory persists, decisions don't go stale.

Downstream tolerance for the resulting absences is the existing, verified behavior catalogued
in Context ("already half-defensive") — this ADR adds no new downstream defaults.

**D3 — Classify failures at the `LLMCaller` boundary into three classes.**

| Class | Members | Gate behavior |
|---|---|---|
| **Transient** (turn-degradable) | `SimulationError.retriesExhausted`; `SimulationError.llmGenerationFailed` wrapping transient stream/generation errors; `LLMError.networkError` (Ollama dev backend — a flaky HTTP call is transient; a dead server trips the D4 breaker within 3 turns, acceptable for a dev-only path) | Skip the turn (D1/D2), count it (D4) |
| **Systemic** (run-fatal) | `LLMError.invalidGrammar` (deterministic engineering bug — existing no-retry rationale), `LLMError.notLoaded` | Rethrow immediately → existing catch-all → `.error` → `.failed` |
| **Control-flow** (never degraded) | `CancellationError` / `Task` cancellation; `LLMError.suspended` (already consumed inside `consumeStreamWithSuspendRetry`, never escapes) | Rethrow immediately (cancellation); n/a (suspended) |

`LLMError.loadFailed` is deliberately **not** in the systemic set: it is thrown only from
`loadModel`, which the App layer calls and handles *before* the run starts — it cannot reach
the `LLMCaller` boundary, and a gate arm for it would be dead code.

Mechanism: today `LLMCaller.swift:98` eagerly wraps every non-retryable stream error —
`invalidGrammar` and `notLoaded` included — into
`SimulationError.llmGenerationFailed(description:)`, erasing the type the gate needs.
`LLMCaller` will rethrow the systemic `LLMError` cases **typed** (unwrapped); the runner
catch-all already wraps any non-`SimulationError` for the `.error` event
(`SimulationRunner.swift:370-374`), so user-facing messages are unchanged on the run-fatal
path. The gate pattern-matches: systemic/cancellation rethrow, everything else degrades.

**D4 — Run-level circuit breaker: 3 consecutive skipped turns → run-fatal.** The gate keeps a
consecutive-skip counter (reset on any successful turn, shared across phases within one run).
When a degradable failure would be the 3rd consecutive skip, the gate instead throws a new
`SimulationError.turnFailureLimitReached(consecutiveCount:)` — surfacing through the existing
abort path with an honest, localized description. Rationale: each skipped turn costs up to
3 attempts of inference latency; without a breaker, a systemically-dead backend that evades
the D3 taxonomy (e.g. every call times out) would grind through the remaining scenario for
tens of minutes emitting nothing — a zombie run is worse UX than an early abort. The counter
is runner-execution state, **not** part of `SimulationState` (a resumed run starts at 0; no
Codable/checkpoint migration). The limit is a named constant with a doc comment, not a
tuning contract — it is a UX bound, model-independent, so no mechanism-contract framing is
needed (`.claude/rules/adr-writing.md` §2 consulted).

Gate mechanics (load-bearing for "shared across phases"): `PhaseContext` is a `Sendable`
struct rebuilt per phase in `executePhases`, so a value-type gate would reset the counter at
every phase boundary and neuter the breaker. The gate is therefore a **`Sendable` reference
type** (`final class` with `OSAllocatedUnfairLock`-protected state — the `SuspendController`
pattern), created once per run by `SimulationRunner` and the **same instance** injected into
every `PhaseContext`. Invariant: handlers that dispatch sub-phases (`ConditionalHandler`)
must thread the parent context's gate into the sub-phase contexts — a fresh gate inside a
branch would silently reset the count.

**D5 — Explicit, informational `SimulationEvent.turnSkipped`.** New case
`turnSkipped(agent: String, phaseType: PhaseType, cause: String)`, modeled on
`.languageMismatch`: informational, not `.error`; the simulation continues. `cause` carries
the diagnostic description (same register as `llmGenerationFailed`'s payload — English,
non-localized); the App layer renders its own localized narration line in the **live** log.
No silent degradation.

Per-turn honesty is **live-only**; the durable post-hoc signal is the aggregate
`degradedTurnCount` (D6), counted App-side from `.turnSkipped` events — the same shape as
the existing `languageMismatchCount`. This follows the `.languageMismatch` precedent exactly:
informational events are not persisted today, and `ReplayViewModel` deliberately no-ops
`.languageMismatch` on replay. Persisting per-turn skip markers (e.g. sentinel `TurnRecord`
rows) was considered and rejected: sentinel rows would pollute every transcript-rendering
consumer of the `turns` table for marginal value once the live narration + aggregate badge
exist. #992's honesty requirement ("no silent degradation") is met by the live narration plus
the post-hoc badge; if field usage later shows per-turn post-hoc placement matters, that is
an additive follow-up (a nullable marker column on `turns`), not a rework.

**D6 — Run status taxonomy unchanged; degradation recorded as metadata.** A run that reaches
`.simulationCompleted` with skipped turns persists as `SimulationStatus.completed` plus a new
additive `SimulationRecord` column `degradedTurnCount INTEGER NOT NULL DEFAULT 0`. Results
and history UI show a badge/annotation when the count is > 0 ("completed — N turns skipped").
A circuit-breaker abort persists `.failed` exactly as today. The five-way `SimulationStatus`
(`SimulationStatus.swift`) is untouched.

**D7 — Retry budget unchanged.** `maxRetries = 2` shared across causes stays, as do the
`suspended` / `invalidGrammar` exemptions. Per-cause budgets are rejected: once a turn
failure is cheap (a skip, not a dead run), extra budget bookkeeping buys little; and #751
explicitly defers retry-budget changes for the empty-output sub-class pending on-device
root-cause. The `empty_field` / `language_mismatch` degraded-return paths are also unchanged
(they already continue; re-plumbing them through `.turnSkipped` would change delivered-output
semantics for no containment gain).
**Superseded in part 2026-08-06** — the containment argument holds, but `Amendment 2026-07-17`
moved this ADR's evaluative axis to honesty, and on that axis an empty *canonical primary*
returned as a normal `.agentOutput` is the same defect `options[0]` was. `empty_field`
exhaustion now throws when the declared schema carries the canonical primary and its value is
empty; the retry budget itself, and the `language_mismatch` path, are still unchanged. See
§ "Amendment 2026-08-06".

**D8 — Resume proposal for failed runs: deferred final increment.** For a `.failed` run
holding a valid round checkpoint (`stateJSON` + `currentRound`), offer "resume from round
N+1" in the results UI, reusing the paused-run resume machinery
(`SimulationRunner.run(resumingFrom:startRound:)`). This rescues the circuit-breaker/systemic
case that D1–D5 deliberately do not contain. It ships last and may be spun off to its own
issue if, after D1–D6 land, field behavior shows it is rarely reachable — decide at PR4
planning, not silently.

## Rationale

- **Per-turn is the natural failure unit.** The existing precedents the issue names —
  tally-drop, `options[0]`, the reflect non-empty guard, `.languageMismatch` — are all
  per-agent/per-value containments, not phase- or run-level ones. (**2026-07-17** — the
  granularity claim stands, but `options[0]` is the one entry that is not a *containment*
  at all: it fabricates. See § "Amendment 2026-07-17".) D1 extends the pattern
  rather than inventing a parallel policy layer.
- **Omission composes with what's already there.** Every downstream consumer was verified to
  tolerate absent inputs (Context). Fabricated defaults would *reduce* honesty and would need
  new per-phase policy definitions; omission needs none.
- **The taxonomy preserves the two existing fail-fast rationales.** `invalidGrammar` and
  cancellation abort exactly as today; the suspend path is untouched. The design changes
  behavior only for the transient class where retry-then-die is currently the sole option.
- **The breaker bounds the worst case in both directions.** Without it, degradation converts
  "fail fast" into "fail slow" for systemic outages; with it, worst-case wasted time is
  bounded at ~3 turns × 3 attempts beyond today's behavior, while a single mid-run flake — the
  common case per #751 — costs one narrated gap instead of the whole run.
- **Status-as-metadata keeps the DB and UI honest without a taxonomy fork.** `completed` still
  means "all rounds ran to the end"; the badge carries the quality signal; `failed` still
  means "did not reach the end". No consumer of `SimulationStatus` needs re-auditing.

## Trade-offs

**Easier:**

- First-user / App Review experience: a transient flake yields a narrated gap, not a dead run.
- Debugging honesty: `.turnSkipped` events + `degradedTurnCount` make flakiness measurable in
  the field (complements #751's quantification).
- Future backends (LiteRT-LM, new models): containment semantics are backend-agnostic;
  nothing pins to Gemma/llama.cpp behavior.

**Harder / costs:**

- Scenario semantics under gaps: a skipped vote can flip an elimination or route a
  `conditional` to its `else` branch. This is accepted — it is exactly what the existing
  absent-variable semantics already do — but transcripts become harder to reason about
  without reading the skip narrations.
- Six handlers gain a gate call and skip bookkeeping; tests must cover per-handler skip
  behavior and the cross-phase breaker. Failure injection uses MockLLMService's **existing**
  `throwErrorOnNextGenerate(_:count:)` API (typed `LLMError` injection — which is also why
  the D3 typed rethrow is a prerequisite for exercising the systemic arm in tests).
- A new `SimulationEvent` case ripples to the ADR-013 harness `EventLineMapper` (its final
  switch tier is `default:`-free — the intended compile canary; the earlier tiers carry
  `default:` and also need a routing decision) and to App-layer event handling,
  `ContentFilter` pass-through, and localization.
- The D3 typed rethrow slightly widens `LLMCaller`'s throw surface (typed `LLMError` escapes
  where today everything is pre-wrapped); the runner catch-all's existing wrap keeps
  user-facing behavior identical, but future callers must not assume `SimulationError`-only.

## Implementation staging (multiple PRs, per #992)

| PR | Scope | Notes |
|---|---|---|
| 1 | Engine core: D3 typed rethrow in `LLMCaller`; `SimulationEvent.turnSkipped` + `SimulationError.turnFailureLimitReached`; `TurnFailureGate` (reference type, D4 mechanics) + `PhaseContext` wiring incl. the `ConditionalHandler` gate-threading invariant; adopt in `speak_all` / `speak_each` incl. their D2 `lastOutputs` clear-on-skip; harness `EventLineMapper` arm; regression tests via existing `MockLLMService.throwErrorOnNextGenerate` | `swift build` (harness) required locally per `.claude/rules/xcodebuild-cli.md` |
| 2 | Remaining handlers: `vote` (abstention), `choose` (pair-drop + individual), `reflect`, `whisper` (early-exchange-end); `lastOutputs` clear-on-skip for `vote` / `choose`; downstream-chain tests (all-ballots-skipped → eliminate no-op → `vote_winner` absent → else branch) | Pure Engine + tests |
| 3 | App/UX: localized skip narration in live log; `degradedTurnCount` migration + badge (post-hoc signal, D5/D6); `terminalStatus` regression tests | i18n per `.claude/rules/i18n.md` (Form B format strings, ja catalog entries) |
| 4 | D8 resume proposal for `.failed` runs with checkpoint | Re-scope decision at planning time (see D8) |

Each PR stays within subagent review budgets (≲800 changed lines). Only PR 4's issue-close
directive: PRs 1–3 reference #992 without `Closes` (multi-PR split convention).

## Non-goals

- Parser robustness fixes and empty-output root-cause (#751 — orthogonal, stays open).
- Model load/reload stability (#680 — closed; systemic class remains fail-fast here).
- Per-cause retry budgets (rejected, D7).
- Any scenario-YAML surface or `PhaseType` change — hence **no `ENGINE_SCHEMA_VERSION` bump**
  (ADR-020's gate keys on scenario capabilities, which are unchanged; this ADR alters
  failure-path behavior only, not the semantics of successful execution).
  **Superseded in part 2026-07-17** — the Amendment below *does* alter the semantics of a
  *successful* turn (a delivered but unmappable action). The conclusion (no bump) survives,
  but on a different argument; see § "Amendment 2026-07-17" → "Why there is still no bump".

---

## Amendment 2026-07-17 — D2 extended to *delivered but unmappable* actions (#1151)

**Supersedes** D2's `choose` (round-robin) bullet, which reads: *"The #599 `options[0]`
fallback is untouched — it handles a *delivered but off-menu* answer; a failed call
delivers nothing to canonicalize."* That exemption was correct about the **boundary** —
a failed call and an off-menu answer are different events — but wrong to conclude the
second needs no treatment. Both fabricate; only the first was fixed.

Also supersedes the "Value" bullet of [ADR-002](ADR-002.md) `Amendment 2026-06-14`,
which names `options[0]` "the correctness floor, unchanged".

### The fabrication

`ChooseHandler.validateAction` (`ChooseHandler.swift:216-218`) is:

```swift
private func validateAction(_ action: String, options: [String]) -> String {
  if options.isEmpty { return action }
  return options.contains(action) ? action : options[0]
}
```

An agent that decides to defect and emits `betray!`, `"Betray"`, or (post-[ADR-027](ADR-027.md)
localization) `裏切る` is **scored as cooperating** — `options[0]`. The engine invents a
decision the agent did not make and reports it as fact. That is the exact thing D1's
"degrade by omission, never fabrication" forbids; the D2 exemption above let it stand
because the call *succeeded*, which is the wrong axis. **Success of the call and mappability
of its content are independent.**

This is not hypothetical. `EventReactivePayoffLogic.swift:53-63` records the observation
that motivated *its* own normalization: *"the model may emit `"Betray"` / `" betray"` for a
`favors: betray` round"*. Two sibling consumers of the same field disagree about whether
variants are routine; the one with a live fabrication is the one that assumed they were rare
(`ChooseHandler.swift:14` — *"the fallback is rare in practice"*).

### Decision

`validateAction` becomes **normalize-then-canonicalize**, returning `String?`:

1. Fold both sides — trim + lowercase, matching `EventReactivePayoffLogic.normalize`
   (`:61-63`). Without this the fix trades frequent fabrication for frequent silent
   scoring loss: `"Betray"` would map to `nil` rather than to `betray`.
2. On a match return the **canonical option string**, not the raw input. The return is
   load-bearing as a *token*, not merely a verdict: `RelationshipUpdateHandler.swift:102`
   does an exact dict lookup `deltas[action]` against YAML `action_deltas` keys, and
   ADR-027's `PairwisePayoffLogic` exact-matches `payoff.when` rows the same way.
3. `nil` **only** on genuine non-membership → the caller **drops the whole pairing**,
   reusing the same `continue`-the-pairing semantics as the D2 drop at
   `ChooseHandler.swift:86`. Note this needs a **second** guard: `:86` runs *before* the
   `validateAction` calls at `:88-89`, so a `nil` return cannot flow into it. A dropped pairing scores
   nothing and emits no `.pairingResult` — omission, not invention.

**Scope: round-robin only.** `validateAction` is called at exactly two sites
(`ChooseHandler.swift:88-89`), both inside `executeRoundRobin`. `executeIndividual`
(`:160-212`) never calls it — it writes the whole raw `output` (action included, uncanonicalized) to `state.lastOutputs` at `:208`.
That asymmetry is deliberate and stays: individual mode does not *fabricate* (it invents
nothing), it merely passes raw values to consumers that normalize on read. This is why
`EventReactivePayoffLogic` carries its own `normalize` at all.

### Observability (load-bearing — the drop must not be silent)

The drop happens **after** a successful `turnGate.attempt`: the call succeeded, the output
parsed, and `.agentOutput` was already emitted (`ChooseHandler.swift:150`). So it emits no
`.turnSkipped` — whose sole emitter is `TurnFailureGate.swift:65` — and therefore never
reaches `degradedTurnCount` (incremented only under `case .turnSkipped`,
`SimulationViewModel.swift:1647-1656`). Worse, every successful-but-dropped turn **resets**
the D4 consecutive-skip counter.

Left unaddressed, a model reliably emitting off-menu actions would drop **100%** of
pairings, score nothing all run, and finish `.completed` with `degradedTurnCount == 0`,
no badge, no breaker trip — a **worse** diagnostic posture than the fabrication being
removed, directly against D6's "Debugging honesty" rationale.

Therefore:

- **A new `SimulationEvent` case** — `actionRejected(agent: String, phaseType: PhaseType,
  raw: String)` — is emitted on every rejection, carrying the raw value so the run log
  shows *what* the model said. Reusing `.turnSkipped` with a distinct `cause` was rejected:
  the turn demonstrably did **not** skip, `.agentOutput` already rendered the agent's
  utterance, and a UI would show "Alice said X" followed by "Alice's turn was skipped".
  It would also give `.turnSkipped` a second emitter, breaking a documented invariant.
- **It folds into the existing `degradedTurnCount`** aggregate and badge — no new column,
  no migration. The badge already means "completed with quality loss", which is exactly true.
- **The D4 breaker is explicitly left untouched.** A rejection is model menu-discipline,
  not infrastructure failure. Interleaved speak-phase successes reset the consecutive
  counter anyway, so feeding rejections in would be dead weight.

**`VoteHandler`'s invalid-ballot drop is deliberately NOT folded in.** It looks like a
sibling asymmetry but is not: `VoteHandler.swift:49-57` drops out-of-candidate votes from
the tally while keeping the raw value in `votes`, and
`.voteResults(votes: [String: String], tallies: [String: Int])`
(`SimulationEvent.swift:114`) emits **both** — so an invalid ballot is already observable
by diffing them. `choose` has no such channel, which is why it needs a new event and
`vote` does not.

### Why there is still no bump

`EngineSchemaVersion` is **not** bumped for this change, but the original Non-goals
rationale ("failure-path behavior only") no longer holds — see the annotation there.

The correct argument: **ADR-020's gate runs in the wrong direction to help.**
`min_engine_version` gates *old app + new content* — it stops a device from running a
scenario its engine cannot execute. This change is *new app + old content*: byte-identical
YAML, new engine behavior. No bump can gate it, because a bump is a claim about capability,
not a request for old behavior. The change ships un-gated by construction.

That is **accepted**, on the D1/D2 grounds this Amendment argues: the prior behavior was
fabrication, and no user is entitled to a fabricated score. It is also why the ordering
constraint below is not negotiable — shipping ADR-027's Japanese options *before* this fix
would widen the fabrication surface precisely when it is most likely to fire.

### Cost

Two distinct costs, often conflated (measured at `8949aef`):

- **Compiler-forced — 4 files** switch over `SimulationEvent` itself and will not build
  until the case is handled: `SimulationViewModel`, `CodePhaseEventPayload+Event`,
  `ReplayViewModel`, and the harness's `EventLineMapper`. Tiered switches may `default:`-
  forward at earlier tiers, but every terminal tier must list the case.
- **A design decision — 7 more files** switch over *downstream projections*
  (`LogEntry.Kind` at `SimulationViewModel.swift:51`, `ReplaySource.Kind`,
  `CodePhaseEventPayload`): `ResultMarkdownExporter`, `ResumeLogReplayMapper`,
  `YAMLReplayExporter`, `SimulationView`, `ResultDetailView+CodePhaseRows`,
  `ModelDownloadHostView+ChatStream`, `ModelDownloadHostView+CodePhaseRows`. The compiler
  forces these **only if** PR2.5 projects `.actionRejected` into their tier — which is a
  choice, not a mechanical consequence. Decide it explicitly: the live simulation log
  should show the rejection (that is the whole point of the event); export/replay surfaces
  are a judgement call.

**`EventLineMapper` is not built by any iOS gate** — the ADR-013 harness is a SwiftPM
package. Per `.claude/rules/xcodebuild-cli.md`, run `swift build` from the repo root before
push or CI's "Harness package build" job fails after a green local suite.

`.claude/rules/engine.md` § "Grammar must not enumerate values" names the `options[0]`
fallback as the runtime constraint and must be swept in the same PR.

### Rollout

One PR, landing **after** ADR-027's engine PR and **before** its preset-migration PR:
`ChooseHandler.validateAction` + the new event case + the **4-file compiler-forced switch
sweep** + the **projection decision for the 7 downstream sites** (§ Cost — a choice, not a
mechanical consequence) + the `engine.md` sweep + tests (including a regression test that an English round-robin
`choose` with a case-variant action still **scores** rather than dropping — the
normalization in step 1 is what makes that pass).

## Amendment 2026-08-06 — D7 reversed for an empty *canonical primary* field (#1290)

**Supersedes** D7's `empty_field` clause, which reads: *"The `empty_field` /
`language_mismatch` degraded-return paths are also unchanged (they already continue;
re-plumbing them through `.turnSkipped` would change delivered-output semantics for no
containment gain)."* The `language_mismatch` half stands — see § "Why `language_mismatch`
does not move in step". Only `empty_field` is reversed, and only for the canonical primary.

### The gap

D7 judged the exclusion on **containment** ("no containment gain"), and on that axis it was
correct: an empty turn already lets the run continue, so routing it through `.turnSkipped`
buys no survivability. But `Amendment 2026-07-17` moved this ADR's own evaluative axis from
containment to **honesty** — `options[0]` was reversed not because it killed runs but because
it invented a decision the agent never made. On that axis the `empty_field` exclusion does not
survive: the exhaustion path writes an empty / `"..."` value to `conversationLog` and
`lastOutputs` and emits a normal `.agentOutput`, so the transcript shows the agent **spoke**
when the model produced nothing. The Context table already names the property — *"Run
survives? Yes (silent)"* — and "silent" is the defect, not the survival.

### Decision

The rule is **two clauses**. Stating them as one sentence conflates the retry trigger with the
exhaustion behaviour, and only the second changes.

1. **Retry trigger — the all-fields scan is unchanged; one case is added.**
   `LLMCaller.hasEmptyFields` keeps scanning *every* field, so an empty secondary
   (`inner_thought` / `reason`) still consumes the retry budget and still emits
   `retryCause cause=empty_field` exactly as today. This clause is called out explicitly
   because the narrowing in clause 2 would otherwise be read as applying to it, silently
   *reducing* inference volume.

   The one addition: a declared canonical primary that is **absent** now also triggers the
   retry. `hasEmptyFields` inspects values, not keys, so an absent key matched nothing and the
   call returned at attempt 0 — handing the handler a primary-less turn to read as `nil`.
   Without this, clause 2's absent case would be unreachable: the loop would never get to a
   final attempt to throw on.
2. **Exhaustion behaviour — changed.** On the final attempt, throw
   `SimulationError.retriesExhausted` — which `TurnFailureGate` already routes to
   `.turnSkipped` — **iff** the phase's *declared output schema contains* the canonical primary
   field **and** that value is absent, empty, or `"..."`. Otherwise the parsed output is
   returned as before.

The canonical primary is `ScenarioConventions.primaryField(for: phase.type)`. It is **not**
derivable from the schema: `OutputSchema` carries no phase identity, and reaching for
`OutputSchema.knownPrimaryKeys` would treat a stray `statement` declared on a `vote` phase as
canonical — the same widening `ScenarioConventions.thoughtField`'s doc-comment already forbids
for the secondary field.

### Why both narrowings are load-bearing

**Primary-only, because an any-field rule omits *delivered* content.** `hasEmptyFields` scans
all fields, so the issue's original proposal would skip a turn carrying a good `statement`
whenever `inner_thought` came back `"..."` — discarding what the model did deliver. That is D2
inverted, not applied: omission is the remedy for fabrication, not a penalty for thin
secondary text.

The narrowing is deliberately **asymmetric**, and the other direction is the accepted cost: an
empty *primary* alongside a delivered *secondary* — an empty `vote` with a paragraph-long
`reason`, an empty `statement` with a real `inner_thought` — is still discarded wholesale. That
is the intended reading of the primary/secondary split: a private thought with no utterance is
not a turn, whereas an utterance with no private thought is.

**Declared-schema-only, because the canonical key is enforced at commit time and never
re-checked.** `ScenarioValidator.validateForCommit` gates the canonical primary when a scenario
is persisted; `SimulationRunner` accepts already-persisted scenarios as-is. So a scenario
written before that gate, or imported under [ADR-020](ADR-020.md) backward-compat, can omit the
canonical key from its `output:` schema entirely. The GBNF grammar never generates an
undeclared key, so a rule keyed on the phase type alone would find it absent on **every**
attempt of **every** turn of that phase — three consecutive skips, `turnFailureLimitReached`,
and a scenario that ran yesterday becomes unrunnable. The precondition is what keeps this
change from being a silent compatibility break.

### What this reclassifies

Six phase types have a non-nil `primaryField` and are reached through `turnGate.attempt`:

| Phase | Empty primary today | After |
|---|---|---|
| `speak_all` / `speak_each` | blank `conversationLog` entry + normal `.agentOutput` | no write, `.turnSkipped` |
| `whisper` | blank utterance emitted with `whisper_to` | no write, `.turnSkipped` (ends that pair's exchange early, per D2) |
| `vote` | empty ballot → out-of-candidate → #524 tally-drop | `.turnSkipped` — abstention, and it now **consumes** circuit-breaker budget |
| `reflect` | blank `.agentOutput`; `notes_<name>` already protected | no write, `.turnSkipped` |
| `choose` | empty action → no option match → pairing dropped + `.actionRejected` | `.turnSkipped` (the turn never reaches the handler) |

`narrate` is excluded **structurally**: `primaryField(for: .narrate)` is `nil` (its schema is
engine-fixed, not author-declared), and it is the one LLM call site not wrapped in
`turnGate.attempt`. Both facts must hold for the exclusion. What breaking the second one costs
depends on whether the un-gated site catches: `NarrateHandler` does, so a throw there would be
swallowed and the round would simply lose its narration — no `.turnSkipped`, no breaker
increment, a degradation the gate never counts. A future un-gated site with **no** catch would
abort the run instead. Only the first fact is test-pinned (`narrateIsStructurallyExcluded`);
the second is carried as a new-phase invariant in `.claude/rules/engine.md` § "Adding a new
`PhaseType`".

**`choose`'s reclassification is a deliberate taxonomy move**, not an accident: an *empty*
action is "produced nothing" (`.turnSkipped`), whereas a *delivered but off-menu* action
remains "delivered, unmappable" (`.actionRejected`, per `Amendment 2026-07-17`). The two now
route differently, and only the first feeds D4's counter.

### Consequences accepted

**Empty-primary turns now consume `TurnFailureGate` circuit-breaker budget.** This is the
actual semantic change — the omission itself is cosmetic by comparison. A model producing
systematically empty primaries (the #751 sampler class was RNG-independent, so retries
reproduced identically) reaches `consecutiveSkipLimit` after three turns and the run ends
`.failed` where it previously completed with a blank transcript. Accepted on the same grounds
D4 already argues for `parse_failed`, whose exhaustion is the identical "three full-latency
attempts, nothing usable" shape: failing after ~9 wasted inferences beats grinding out ten
rounds of blanks. The mitigation is **D8's failed-run resume**, which is shipped
(`ResultDetailView`'s resume banner), so such a run is recoverable rather than lost.

**An absent declared primary now costs a full retry budget.** Clause 1 widens the retry trigger
so a *missing* canonical key — not just an empty one — consumes all three attempts instead of
returning at attempt 0. That is a prerequisite (without it the exhaustion leg is unreachable),
but it is 2 extra inferences per affected turn on any backend where an undeclared-key omission
is possible (Ollama `format:json`, `MockLLMService`, any non-GBNF path). It cannot loop — the
`for attempt in 0...maxRetries` bound is untouched — so the worst case is bounded at three
full-latency attempts followed by a skip, and three such turns end the run via the breaker
above. Under GBNF the case is unreachable in practice: the grammar always emits the declared keys.

**More runs suppress the App Store review prompt.** `ReviewRequestPolicy` gates on
`degradedTurnCount == 0`, and these turns now increment it. Desirable — the run genuinely lost
content — but it is a user-visible coupling worth naming.

**No `EngineSchemaVersion` bump**, for the reason `Amendment 2026-07-17` § "Why there is still
no bump" gives verbatim: ADR-020's gate runs old-app-plus-new-content, and this is
new-app-plus-old-content. It ships un-gated by construction.

### Why `language_mismatch` does not move in step

A wrong-language utterance is **delivered content**. Omitting it would destroy real signal, and
the fabrication axis this Amendment reasons on simply does not apply — nothing was invented.
`language_mismatch` exhaustion keeps its informational-event-plus-return shape. The engines'
existing ordering already satisfies the priority this needs: the empty-field arm runs *before*
the language-adherence check, so a final attempt that is both wrong-language and empty-primary
skips rather than emitting `.languageMismatch` alongside an empty return.

### What is deliberately *not* changed

- **The all-keys guard on the repair / salvage acceptance paths.** `JSONResponseParser`
  requires every expected key present and non-empty before accepting a *repaired* or
  *salvaged* object. That is a repair-acceptance criterion — "do not trust a reconstruction
  that lost fields" — not content discard, and it stays all-keys **in Swift**. Kotlin has
  neither acceptance path today (and, after this Amendment, no `hasAllExpectedKeys` helper), so
  for Kotlin this is the shape the Stage-3 port must reproduce, not a current state.
- **`ReflectHandler`'s non-empty `notes_<name>` guard.** It remains, but this Amendment
  **demotes it from precedent to defense-in-depth**: § "The codebase is already half-defensive"
  cites it as evidence that empty values were already tolerated by omission, and for a
  *declared* `note` the exhaustion path can no longer deliver an empty value to it at all. The
  guard still covers the undeclared-schema path.

### Cross-engine convergence (ADR-023)

The two engines already diverged here, and this Amendment resolves it. Swift's schema guard is
consulted only on the salvage / post-repair paths, so an empty declared key reaches the
`empty_field` path; Kotlin applied the same guard on **every** successful parse, so an empty
declared key became `parse_failed` and the turn skipped. That divergence is recorded as
`DivergenceLedger.SCHEMA_GUARD_POSITION` and documented in `.claude/rules/kmp-interop.md`
§ Pattern 4.

Kotlin converges by **deleting** its post-parse guard — it has no salvage or post-repair path
to move the guard to (both are "Knowingly absent … Stage 3" in that parser's own class
comment), and its `expectedKeys` parameter is retained solely as a reservation for the Stage-3
repair port. Both engines then return the parsed output on the happy path and throw at
exhaustion under the rule above. Note that Kotlin's guard used `isNullOrEmpty`, so the `"..."`
sentinel already fell through to its `empty_field` path — the divergence was never total.

Resolving it retires the ledger case, which **disarms the structural arm** of the parity
negative control (`ParityFixtureEmitter`'s divergent fixture exists to drive one divergence of
each kind). Re-arming that arm, or recording the gap, is a condition of this change — not a
follow-up to be discovered later.

### Cost

- **`LLMCaller.call` gains a parameter** carrying the phase identity, on both engines — 8 Swift
  call sites and 8 Kotlin ones. `LLMCaller` is `internal`, so no public-signature gate fires.
- **No new `SimulationEvent` case.** `.turnSkipped` already carries these semantics; adding a
  sibling would duplicate it minus D4 participation, and its `.actionRejected` precedent does
  not transfer (that case exists precisely *because* `.agentOutput` had already rendered).
  `EventLineMapper` therefore needs no change, and neither do the compiler-forced switch sets
  § Cost above enumerates.
- **Parity assets**: fixture regeneration, ledger-case deletion, and the `kmp-interop.md`
  § Pattern 4 rewrite (both its guard-position and its **Apply** paragraph, whose reasoning
  changes and which now applies symmetrically to Swift).

### Rollout

One PR, four commits — ADR / Swift + tests / Kotlin (guard deletion and the caller rule are
**atomic**: deleting the guard alone would leave Kotlin returning the empty output, a new
undocumented divergence) / parity assets. Verification must include reverting the new branch to
confirm the positive test reddens, and `pastura-harness parity-emit --check`, which at the time
no CI job or hook ran — hence running it by hand. It has since been wired into the
`harness-build` job (#1399), so that step is now covered per-PR.
